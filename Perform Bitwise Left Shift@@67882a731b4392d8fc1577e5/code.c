#include <stdio.h>

int main() {
    int num,shift;
    scanf("%d %d ",&a,&shift);
    int result;
    result= num<<shift;

    printf("%d",result);
    printf("%s", welcome());
    return 0;
}